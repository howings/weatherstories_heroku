{
  "name": "weather-stories",
  "version": "1.0.0",
  "description": "this is a lovely interactive weather movie app",
  "version": "1.0.0",
  "engines": {
    "node": "14.15.4"
  },
  "main": "index.js",
  "scripts": {
    "build": "cd client && npm run build",
    "install-client": "cd client && npm install",
    "heroku-postbuild": "npm run install-client && run build",
    "start": "node index.js"
    
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "config": "^3.3.6",
    "cors": "^2.8.5",
    "curl": "^0.1.4",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "helmet": "^4.4.1",
    "joi": "^17.4.0",
    "mongoose": "^5.12.2",
    "morgan": "^1.10.0"
  }
}
